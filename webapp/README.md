At the beggining our idea was to use app.py to have a live demo with streamlit online but with the weights our models had at the end we decided to have 2 local demos, one for each model.

You can also use the local streamlit demos if you get the models from random-forest.ipynb and roberta.ipynb from the notebooks folder (I reccomend to check also if your local works well with relative paths or you have change it to your absolute paths).

You can also see part of our process in web.py.