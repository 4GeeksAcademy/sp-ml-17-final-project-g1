Here you only have the encoder of the emotions that we use to go from the emotions in text to a number that represents them and the text vectorized that helped us to use a random forest model vectorizing the text to numerical vectors and allowing us with that to train that model (it's also used with the random forest deploy in local_forest.py)

If you want to have here the random forest model that we use in local_forest.py you will have to run the notebook named random-forest.ipynb from the notebooks folder.

If you want to have here the RoBERTa model that we use in local_RoBERTa.py you will have to run the notebook named roberta.ipynb from the notebooks folder.